---
title: "QA-C Static Analysis Configuration (MISRA)"
description: "QA-C Static Analysis Configuration (MISRA) (QAC) — Static-analysis configuration for QA-C (analyser presets, profiles, per-module projects) plus the project MISR"
---

# QA-C Static Analysis Configuration (MISRA)

*Repository directory: `QAC`*

:::note[Origin: Tooling configuration]
QA-C / MISRA configuration for static analysis; no ECU code. See `QAC/doc/`.
:::

## Purpose and responsibility

Static-analysis configuration for QA-C (analyser presets, profiles, per-module projects) plus the project MISRA Compliance Guidelines document; no ECU code.

## Key files

## Public API

No C function definitions were detected in this module (it may ship headers, libraries, configuration or tooling only).

## Dependencies

No project-internal quoted includes detected (self-contained or header-only at this level).

## Documents

The module ships 1 Word/PDF/text documentation file(s). Summaries and conversion notes live on the documents page:

- [Design and reference documents](./documents/)

Source files:

- `QAC/doc/MISRA Compliance Guidelines.docx`
